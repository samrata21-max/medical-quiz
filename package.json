{
  "name": "neurology-mcq",
  "version": "1.0.0",
  "private": true,
  "description": "Neurology MCQ learning app",
  "scripts": {
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync",
    "cap:open:android": "cap open android",
    "cap:run:android": "cap run android",
    "cap:copy": "cap copy"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.0",
    "@capacitor/android": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}